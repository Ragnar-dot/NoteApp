name: noteapp
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  hive: ^2.0.0
  hive_flutter: ^1.1.0
  path_provider: ^2.0.9
  google_fonts: ^4.0.3  # Füge die Google Fonts Abhängigkeit hinzu
  json_theme_plus: ^6.6.1
   
   
  flutter_localizations:
    sdk: flutter
  intl: 0.19.0
  flutter_neumorphic: ^3.2.0
  gradient_ui_widgets: ^1.0.2-nullsafety

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.0.0
  build_runner: ^2.0.0
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.9.2

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:

    - assets/appainter_theme.json
   
    - assets/icon/icon/app_icon_background.png
    - assets/icon/icon/app_icon_foreground.png
    
    - assets/lang/en.json
    - assets/lang/de.json
    - assets/lang/pl.json   

flutter_icons:
  android: true
  ios: true
  
  adaptive_icon_background: "assets/icon/icon/app_icon_background.png"     # Hintergrund für das adaptive Icon (Android 8.0+)
  adaptive_icon_foreground: "assets/icon/icon/app_icon_foreground.png"  # Optional, für Android